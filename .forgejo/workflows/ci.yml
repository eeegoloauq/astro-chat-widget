# The pre-merge check, and the only thing this repository asks of the local
# runner: pull requests live in Forgejo, so the gate has to be here. Everything
# after the merge — main, tags, releases, publishing — runs on GitHub, where the
# copy of this file in .github/workflows does the same work for free.
#
# NB: because this directory exists, Forgejo ignores .github/workflows entirely.
name: ci

on:
  pull_request

permissions:
  contents: read

# A newer push to the same branch makes the running check pointless.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: docker
    # Node comes with the image and the npm cache stays in a named volume:
    # setup-node downloaded both on every run, which was most of the check.
    container:
      image: node:22-bookworm
      volumes:
        - ci-npm-cache:/root/.npm
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      # astro check type-checks the component and the demo that consumes it,
      # which is the whole of what this package can be checked against.
      - run: npm run check
      - run: npm run build
